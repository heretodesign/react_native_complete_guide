import React from 'react'
import { View, Text, StyleSheet } from 'react-native'

const ListScreen = () => {
    return <Text>ListScreen Screen</Text>
};

const styles = StyleSheet.create({});

export default ListScreen;